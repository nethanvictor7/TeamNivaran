# ADR 0009: Deterministic field extraction

Optional extraction rules map safe JSONPath values to a fixed set of integration
fields with allowlisted transforms. No expressions, business-event definitions,
custom event schemas, or executable source logic are accepted.

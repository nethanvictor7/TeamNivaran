# ADR 0019: Immutable decision Evidence snapshots

Reviews, recommendations, and decisions pin exact Evidence Version IDs and
SHA-256 hashes. A later or replaced version is never silently substituted.
Content, storage keys, signed URLs, and unrestricted metadata are excluded.
